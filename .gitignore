# dependencies & build output
node_modules/
dist/
build/

# runtime binaries (downloaded / built, not source)
cloudflared.exe
Launchpad.exe
Launchpad-Setup*.exe

# scratch icon at the repo root (the README uses assets/icon-256.png)
/icon-256.png

# per-user runtime data (project ports/paths, logs) — regenerated at runtime
config.json
launchpad-error.log
*.log
